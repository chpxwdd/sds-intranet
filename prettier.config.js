module.exports = {
	printWidth: 80,
	singleQuote: true,
	//-----------------
	// trailingComma
	// -- 'all' — 	обеспечивает наличие запятой после последнего свойства объекта
	// -- 'none' no trailing commas.
	// -- 'es5' Trailing commas wherever possible (including function arguments)
	trailingComma: 'none',
	bracketSpacing: true, // отвечает за вставку пробелов между телом объекта и фигурными скобками в объектных литералах.
	jsxBracketSameLine: false, // благодаря этому правилу символ > в многострочных JSX-элементах будет помещён в последней строке.
	tabWidth: 2,
	useTabs: true,
	semi: true
}
